# Internal working material — never committed to the public repo (local-only)
refs/
.omo/
docs/superpowers/

# OS / editor
.DS_Store
*.log
node_modules/

# Build / test output (electron-vite, electron-builder, playwright captures, etc.)
out/
dist/
output/
*.dmg
